export interface HeaderState {
  auth: {
    open: boolean;
    tab: number;
  };
  contactUs: {
    open: boolean;
  };
  categories: {
    open: boolean;
  };
  confirmed: {
    open: boolean;
  };
}
